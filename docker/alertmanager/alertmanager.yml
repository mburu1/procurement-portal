global:
  resolve_timeout: 5m

route:
  receiver: 'team-notifications'
  group_by: ['alertname', 'job']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: 'team-notifications'
    # configure at least one supported receiver below
    # - email_configs:
    #     - to: 'oncall@example.com'
    #       from: 'alertmanager@example.com'
    #       smarthost: 'smtp.example.com:587'
    #       auth_username: 'alertmanager'
    #       auth_password: 'REPLACE_ME'
    # - slack_configs:
    #     - api_url: 'https://hooks.slack.com/services/T/REPLACE/ME'
    #       channel: '#alerts'

inhibit_rules: []
